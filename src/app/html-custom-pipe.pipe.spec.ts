import { HtmlCustomPipePipe } from './html-custom-pipe.pipe';

describe('HtmlCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
